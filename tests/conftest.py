"""Shared pytest fixtures for CritCom tests.

Add fixtures as they're needed. Fixtures for FHIR, LLM clients, and the
HAPI integration server live here so every test file can reuse them.
"""

from __future__ import annotations
